name: ssp_libya_env_3 # you can adjust the name 
channels:
  - conda-forge
dependencies:
  - python=3.11
  - matplotlib=3.9.2
  - xlsxwriter=3.1.1
  - ipykernel=6.29.5
  - setuptools>=60,<70
  - pip
  - setuptools>=60,<70 # otherwise install it directly on the terminal /opt/anaconda3/envs/ssp_libya_env_2/bin/python -m pip install "setuptools>=60,<70"
  - pip:
      - git+https://github.com/jcsyme/sisepuede.git
      - ssp-transformations-handler
      - costs-benefits-ssp
      - polars
